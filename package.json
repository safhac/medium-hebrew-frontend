{
  "name": "medium-hebrew-frontend",
  "description": "Medium with Hebrew support ",
  "version": "0.0.1",
  "license": "MIT",
  "author": "Asaf Cohen",
  "repository": {
    "type": "git",
    "url": "git@github.com:safhac/medium-hebrew-frontend.git"
  },
  "scripts": {
    "start": "elm-live src/elm/Main.elm --output=src/static/elm.js --dir=src/static --open",
    "build": "webpack",
    "reinstall": "npm i rimraf && rimraf node_modules && npm uninstall -g elm && npm i -g elm && npm i && elm package install"
  },
  "devDependencies": {
    "elm-live": "^2.7.5"
  }
}
